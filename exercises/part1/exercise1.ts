{
  // Make sure this assignment succeeds
  const x: (number | string)[] = ['hello', 'world', 42];
  const y: [string, string, number] = ['hello', 'world', 42];
  const z: Array<number | string> = ['hello', 'world', 42];
}
